import React from "react";

export default function ToolSeoSection({ tool }) {
  // This component can be expanded to inject meta tags or structured data
  // based on the specific 'tool' prop passed to it.
  return null;
}
